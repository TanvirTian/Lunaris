# Copy to .env and fill in POSTGRES_PASSWORD before running docker compose.
#
#   cp .env.example .env
#   docker compose up -d --build
#
# This file is NOT the backend application config (that's backend/.env).
POSTGRES_USER=postgres
POSTGRES_PASSWORD=yourpassword
POSTGRES_DB=privacy_analyzer
WORKER_CONCURRENCY=2
