# Frontend Environment — Vite
# Vite only exposes variables prefixed with VITE_ to the browser bundle.
# Any variable without VITE_ prefix is invisible to client-side code.
VITE_API_URL=
VITE_APP_NAME=Lunaris
